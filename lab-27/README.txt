Lab 27 – Building a Simple React Application with Routing and Components

This project includes:
- Basic routing using React Router.
- Three top-level routes: Home, About, Dashboard.
- A nested component structure under Dashboard:
  - UserStats.js
  - RecentActivity.js

Instructions:
1. Run `npm install` to install dependencies.
2. Run `npm start` to view the app at http://localhost:3000.

Ensure React Router is installed:
  npm install react-router-dom

Files cleaned up:
- Removed: App.css, App.test.js, logo.svg, reportWebVitals.js

Component Files:
- App.js (handles routes)
- Home.js, About.js (individual pages)
- Dashboard.js (parent component)
- UserStats.js, RecentActivity.js (nested children)
